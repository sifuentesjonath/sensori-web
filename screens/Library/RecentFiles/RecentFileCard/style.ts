import { CSSObject } from '@mui/styled-engine-sc';
import colorPalette from '@styles/color-palette';

export const recentFileCard: CSSObject = {
	border: `1px solid ${colorPalette.textBlue}`,
	// borderColor: ,
	borderRadius: '16px',
	backgroundColor: 'inherit',
	p: 2,
	width: '200px',
};

export default recentFileCard;
